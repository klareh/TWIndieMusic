import React from 'react';
import TextField from 'material-ui/TextField';

export default () => (
  <div>
    <TextField
      id="search"
      label="Search field"
      type="search"
      placeholder="想找哪邊的呢？"
    />
  </div>
);
